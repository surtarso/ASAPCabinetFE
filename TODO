TODO - From urgent to least important.

Main:
- look for mp4 videos, fallback to .png images, fallback to default image.

UI:
- add 'settings button' and panel
- add 'search button' by query

Flares:
- add sound for table load (some sort of machine loading sound)
- add 'jump to letter'  navigation
- add blinking arrows on sides to indicate table flow/keys
- add splash screen on start
- add video default media
